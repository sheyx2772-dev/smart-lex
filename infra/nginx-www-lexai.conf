# SmartLex WEB — asosiy domen (www.lexai.com.uz + lexai.com.uz → :3000).
# /etc/nginx/sites-available/www.lexai.com.uz  → sites-enabled ga symlink.
# TLS: certbot --nginx -d www.lexai.com.uz -d lexai.com.uz

server {
    listen 80;
    server_name www.lexai.com.uz lexai.com.uz;
    client_max_body_size 25m;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        "upgrade";
        proxy_read_timeout 60s;
    }
}
