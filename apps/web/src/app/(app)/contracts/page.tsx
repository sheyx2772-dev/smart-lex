import { ContractsHub } from "@/components/contracts/contracts-hub";

/**
 * Shartnomalar markazi — TrustContract (trustme.uz) uslubida: shablonlar
 * galereyasi + hamkor kontragent bilan masofadan imzolash so'rovi (SMS / E-IMZO
 * / Telegram). Yangi qarzdorlik shartnomasi yaratish /contracts/new da qoladi.
 */
export default function ContractsPage() {
  return <ContractsHub />;
}
